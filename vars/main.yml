---
# vars file for dnsmasq
dnsmasq_packages:
  - dnsmasq

dnsmasq_service: dnsmasq

dnsmasq_configuration_file: /etc/dnsmasq.conf

dnsmasq_user: root
_dnsmasq_group:
  RedHat: dnsmasq
  Debian: root

dnsmasq_group: "{{ _dnsmasq_group[ansible_os_family] }}"
